spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://192.168.93.23:3306/gulimall_wms
    driver-class-name: com.mysql.jdbc.Driver
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 # nacos服务地址
  application:
    name: guli-ware

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
      logic-delete-value: 1
      logic-not-delete-value: 0

server:
  port: 11000
